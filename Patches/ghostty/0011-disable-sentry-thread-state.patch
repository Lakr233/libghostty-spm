diff --git a/src/Surface.zig b/src/Surface.zig
index 9fbcc2ccf..0224f9ab2 100644
--- a/src/Surface.zig
+++ b/src/Surface.zig
@@ -2476,8 +2476,8 @@ fn queueRender(self: *Surface) !void {
 
 pub fn sizeCallback(self: *Surface, size: apprt.SurfaceSize) !void {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     const new_screen_size: rendererpkg.ScreenSize = .{
         .width = size.width,
@@ -2537,8 +2537,8 @@ fn balancePaddingIfNeeded(self: *Surface) void {
     // log.debug("text preeditCallback value={any}", .{preedit_});
 
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     self.renderer_state.mutex.lock();
     defer self.renderer_state.mutex.unlock();
@@ -2670,8 +2670,8 @@ pub fn keyCallback(
     }
 
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     // Setup our inspector event if we have an inspector.
     var insp_ev: ?inspectorpkg.KeyEvent = if (self.inspector != null) ev: {
@@ -3291,8 +3291,8 @@ fn keyEncoderOptions(self: *Surface) input.key_encode.Options {
 /// this will filter newlines to '\r'.
 pub fn textCallback(self: *Surface, text: []const u8) !void {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     try self.completeClipboardPaste(text, true);
 }
@@ -3302,8 +3302,8 @@ pub fn textCallback(self: *Surface, text: []const u8) !void {
 /// is not visible, and also re-render when it becomes visible again.
 pub fn occlusionCallback(self: *Surface, visible: bool) !void {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     _ = self.renderer_thread.mailbox.push(.{
         .visible = visible,
@@ -3313,8 +3313,8 @@ pub fn occlusionCallback(self: *Surface, visible: bool) !void {
 
 pub fn focusCallback(self: *Surface, focused: bool) !void {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     // Always update the app focused surface, otherwise we miss
     // the first surface created.
@@ -3401,8 +3401,8 @@ pub fn focusCallback(self: *Surface, focused: bool) !void {
 
 pub fn refreshCallback(self: *Surface) !void {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     // The point of this callback is to schedule a render, so do that.
     try self.queueRender();
@@ -3438,8 +3438,8 @@ pub fn scrollCallback(
     // log.info("SCROLL: xoff={} yoff={} mods={}", .{ xoff, yoff, scroll_mods });
 
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     // Always show the mouse again if it is hidden
     if (self.mouse.hidden) self.showMouse();
@@ -3612,8 +3612,8 @@ pub fn scrollCallback(
 /// can then update any DPI-sensitive state.
 pub fn contentScaleCallback(self: *Surface, content_scale: apprt.ContentScale) !void {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     // Calculate the new DPI
     const x_dpi = content_scale.x * font.face.default_dpi;
@@ -3764,8 +3764,8 @@ pub fn mouseButtonCallback(
     mods: input.Mods,
 ) !bool {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     // log.debug("mouse action={} button={} mods={}", .{ action, button, mods });
 
@@ -4461,8 +4461,8 @@ pub fn mousePressureCallback(
     pressure: f64,
 ) !void {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     // We don't currently use the pressure value for anything. In the
     // future, we could report this to applications using new mouse
@@ -4525,8 +4525,8 @@ pub fn cursorPosCallback(
     mods: ?input.Mods,
 ) !void {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     // log.debug("cursor pos x={} y={} mods={?}", .{ pos.x, pos.y, mods });
 
@@ -4714,8 +4714,8 @@ pub fn cursorPosCallback(
 /// changed.
 pub fn colorSchemeCallback(self: *Surface, scheme: apprt.ColorScheme) !void {
     // Crash metadata in case we crash in here
-    crash.sentry.thread_state = self.crashThreadState();
-    defer crash.sentry.thread_state = null;
+    crash.sentry.setThreadState(self.crashThreadState());
+    defer crash.sentry.setThreadState(null);
 
     const new_scheme: configpkg.ConditionalState.Theme = switch (scheme) {
         .light => .light,
diff --git a/src/crash/sentry.zig b/src/crash/sentry.zig
index 26428c0f8..301062dc0 100644
--- a/src/crash/sentry.zig
+++ b/src/crash/sentry.zig
@@ -35,7 +35,12 @@ pub const ThreadState = struct {
 
 /// See ThreadState. This should only ever be set by the owner of the
 /// thread entry function.
-pub threadlocal var thread_state: ?ThreadState = null;
+threadlocal var thread_state: ?ThreadState = null;
+
+pub fn setThreadState(value: ?ThreadState) void {
+    if (comptime !build_options.sentry) return;
+    thread_state = value;
+}
 
 /// Process-wide initialization of our Sentry client.
 ///
diff --git a/src/renderer/Thread.zig b/src/renderer/Thread.zig
index 9bd5beb54..d194184bc 100644
--- a/src/renderer/Thread.zig
+++ b/src/renderer/Thread.zig
@@ -231,11 +231,11 @@ fn threadMain_(self: *Thread) !void {
     }
 
     // Setup our crash metadata
-    crash.sentry.thread_state = .{
+    crash.sentry.setThreadState(.{
         .type = .renderer,
         .surface = self.renderer.surface_mailbox.surface,
-    };
-    defer crash.sentry.thread_state = null;
+    });
+    defer crash.sentry.setThreadState(null);
 
     // Setup our thread QoS
     self.setQosClass();
diff --git a/src/termio/Exec.zig b/src/termio/Exec.zig
index b642a85b8..0d5f3071a 100644
--- a/src/termio/Exec.zig
+++ b/src/termio/Exec.zig
@@ -1396,11 +1396,11 @@ pub const Posix = struct {
         }
 
         // Setup our crash metadata
-        crash.sentry.thread_state = .{
+        crash.sentry.setThreadState(.{
             .type = .io,
             .surface = io.surface_mailbox.surface,
-        };
-        defer crash.sentry.thread_state = null;
+        });
+        defer crash.sentry.setThreadState(null);
 
         // Set the fd to non-blocking so the gather stage can drain it
         // in a tight loop and fall back to poll for readiness. The
@@ -1756,11 +1756,11 @@ pub const Windows = struct {
         defer posix.close(quit);
 
         // Setup our crash metadata
-        crash.sentry.thread_state = .{
+        crash.sentry.setThreadState(.{
             .type = .io,
             .surface = io.surface_mailbox.surface,
-        };
-        defer crash.sentry.thread_state = null;
+        });
+        defer crash.sentry.setThreadState(null);
 
         var buf: [1024]u8 = undefined;
         while (true) {
diff --git a/src/termio/Thread.zig b/src/termio/Thread.zig
index 8e2a21f65..07f7e3582 100644
--- a/src/termio/Thread.zig
+++ b/src/termio/Thread.zig
@@ -244,11 +244,11 @@ fn threadMain_(self: *Thread, io: *termio.Termio) !void {
     }
 
     // Setup our crash metadata
-    crash.sentry.thread_state = .{
+    crash.sentry.setThreadState(.{
         .type = .io,
         .surface = io.surface_mailbox.surface,
-    };
-    defer crash.sentry.thread_state = null;
+    });
+    defer crash.sentry.setThreadState(null);
 
     // Get the mailbox. This must be an SPSC mailbox for threading.
     const mailbox = switch (io.mailbox) {
